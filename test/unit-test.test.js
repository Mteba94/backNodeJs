/*
const assert = require('chai').assert

function addValue(num1, num2) {
  return num1 + num2
}

describe('Suite de pruebas para nuestra api', () => {
  it('esperamos un 10', () => {
    let data = addValue(10, 5)
    assert.equal(data, 10)
  })
})


*/
